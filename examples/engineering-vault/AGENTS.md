# Cairn Agent Guide

Before answering questions that may rely on saved knowledge, search this vault:

```bash
cairn search "<query>" --path examples/engineering-vault --json
```

Open only the relevant document or section. Prefer updating existing notes over
creating duplicates, and never write secrets to the vault.
